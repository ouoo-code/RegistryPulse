import { describe, expect, it } from 'vitest'
import { renderContainerdMirrors, renderDockerMirrors, renderOnePanelMirrors, renderPodmanMirrors, renderRegistryPullCommands } from './config-generator'
describe('config generator', () => { it('deduplicates and limits Docker mirrors', () => { const result = JSON.parse(renderDockerMirrors(['https://a.example', 'https://a.example'])); expect(result['registry-mirrors']).toHaveLength(1) }); it('renders 1Panel Docker runtime JSON', () => { expect(JSON.parse(renderOnePanelMirrors(['https://mirror.example']))['registry-mirrors']).toEqual(['https://mirror.example']) }); it('renders valid Podman host entries', () => { expect(renderPodmanMirrors(['https://mirror.example']).includes('mirror.example')).toBe(true) }); it('renders category-aware Podman registries.conf', () => { const result = renderPodmanMirrors(['https://mirror.example/prefix'], 'ghcr'); expect(result).toContain('prefix = "ghcr.io"'); expect(result).toContain('location = "mirror.example/prefix"') }); it('renders Containerd hosts configuration', () => { expect(renderContainerdMirrors(['https://mirror.example']).includes('[host."https://mirror.example"]')).toBe(true) }); it('renders prefix pull commands for non-Docker Hub registries', () => { const result = renderRegistryPullCommands(['https://ghcr.mirror.example'], 'ghcr'); expect(result).toContain('docker pull ghcr.mirror.example/user/image:tag'); expect(result).toContain('docker tag ghcr.mirror.example/user/image:tag ghcr.io/user/image:tag') }) })
